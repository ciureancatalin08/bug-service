spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jbc:mysql://mysql:3306/reporting
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    database: mysql
    hibernate:
      ddl-auto: create
server:
  port: '8282'
